/**
 * <%= titleCase %> Middleware
 * <%= description %>
 * @author <%= name %> <<%= email %>>
 */

const <%= camelCase %> = (store: any) => (next: any) => (action: any) => {

  return next(action);

};

export default <%= camelCase %>;