/**
 * <%= titleCase %> Middleware
 * <%= description %>
 * @author <%= user_name %> <<%= email %>>
 */

const <%= camelCase %> = store => next => action => {

  return next(action);

};

export default <%= camelCase %>;