const defaultState = {
  initialized: false
};

/**
 * <%= titleCase %> Reducer
 * <%= description %>
 * @author <%= name %> <<%= email %>>
 */

const <%= camelCase %> = (state = defaultState, action) => {
  switch(action.type){
    // Sample:
    // ==============================
    // case DO.INITIALIZE:
    //   return {
    //     ...state,
    //     initialized: true
    //   };
    default:
      return state;
  }
};

export default <%= camelCase %>;